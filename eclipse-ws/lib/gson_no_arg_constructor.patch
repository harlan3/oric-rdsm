Index: MappedObjectConstructor.java
===================================================================
--- MappedObjectConstructor.java	(revision 730)
+++ MappedObjectConstructor.java	(working copy)
@@ -78,7 +78,15 @@
       Class<T> clazz = (Class<T>) Types.getRawType(typeOfT);
       Constructor<T> constructor = getNoArgsConstructor(clazz);
       if (constructor == null) {
-        return (T) THE_UNSAFE.allocateInstance(clazz);
+	 try {
+            // This is a hack to deal with some autogenerated
+            // classes that do not contain a no-args constructor.
+            Class<?> _class = Types.getRawType(typeOfT);
+            T childObj = (T)_class.getMethod("from_int", int.class).invoke(_class, 0);
+            return childObj;
+         } catch (Exception e2) {
+           return (T) THE_UNSAFE.allocateInstance(clazz);
+         }
       }
       return constructor.newInstance();
     } catch (InstantiationException e) {
